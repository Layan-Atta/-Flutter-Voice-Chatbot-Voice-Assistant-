# 💬 Flutter Voice Chatbot (Voice Assistant)

## وصف المشروع

هذا المشروع يهدف إلى بناء مساعد صوتي تفاعلي (Voice Chatbot) باستخدام إطار العمل **Flutter** للواجهات الأمامية، وتوظيف النماذج اللغوية الضخمة (LLMs) لتوليد الاستجابات الذكية. يتميز هذا التطبيق بقدرته على معالجة المدخلات الصوتية وتحويلها إلى استجابات صوتية، مما يوفر تجربة محادثة طبيعية وسريعة.

**حالة المشروع:** جاري العمل على تنفيذ الوظائف الأساسية.

## 🎯 الأهداف الرئيسية للمهمة

1.  **تصميم تطبيق Flutter** بواجهة مستخدم بسيطة تحتوي على زر تفعيل المايكروفون.
2.  **تكامل ثلاث مراحل رئيسية لمعالجة الصوت:**
    * **Speech-to-Text:** تحويل صوت المستخدم إلى نص.
    * **Response Generation:** توليد رد منطقي على النص باستخدام LLM API.
    * **Text-to-Speech:** تحويل الرد النصي من LLM إلى صوت مسموع للمستخدم.
3.  **قياس الأداء:** حساب متوسط وقت الاستجابة (Average Response Time) من النموذج اللغوي لتقييم سرعة التطبيق ومقارنته بالإصدارات المكتوبة بلغات أخرى (مثل بايثون).
4.  **نشر الكود** على GitHub مع ملف `README.md` مفصل يغطي جميع الخطوات والنتائج.


## ⚙️ المتطلبات التقنية والمكتبات (Dependencies)

لإنجاز هذا المشروع، يتطلب الأمر إضافة وتفعيل الصلاحيات والمكتبات التالية في ملفي `pubspec.yaml` و `AndroidManifest.xml` (لمنصة Android):

### 1. الصلاحيات المطلوبة (Permissions)

يجب إضافة الصلاحيات التالية في ملف `AndroidManifest.xml` للسماح للتطبيق بالوصول إلى المايكروفون والإنترنت:

```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
```
### 2. مكتبات Flutter الأساسية (Pubspec.yaml)

نحتاج إلى المكتبات التالية لتنفيذ وظائف الصوت، والاتصال بالـ API:

| المكتبة (Package) | الوصف |
| :--- | :--- |
| `speech_to_text` | لتنفيذ وظيفة **Speech-to-Text** (تحويل صوت المستخدم إلى نص). |
| `flutter_tts` | لتنفيذ وظيفة **Text-to-Speech** (تحويل رد البوت النصي إلى صوت). |
| `http` | لإرسال طلبات **API** إلى النموذج اللغوي (LLM) واستقبال الرد. |
| `provider` أو `flutter_bloc` | (اختياري) لإدارة حالة التطبيق (State Management) بشكل فعال. |



## 💻 خطوات التنفيذ (Implementation Steps)

### الخطوة 1: تهيئة البيئة والواجهة

1.  إنشاء مشروع Flutter جديد.
2.  إضافة المكتبات المذكورة أعلاه في ملف `pubspec.yaml`.
3.  إضافة الصلاحيات المطلوبة في ملف `AndroidManifest.xml`.
4.  تصميم واجهة مستخدم بسيطة (Scaffold) تحتوي على:
    * منطقة لعرض حالة المحادثة (جاري الاستماع...، جاري المعالجة...، الرد الصوتي).
    * زر كبير لتفعيل تسجيل الصوت.

### الخطوة 2: تسجيل الصوت وتحويله (Speech-to-Text)

1.  عند الضغط على الزر، تفعيل ميزة الاستماع باستخدام مكتبة `speech_to_text`.
2.  تسجيل صوت المستخدم حتى يتوقف أو يضغط على الزر مرة أخرى.
3.  استقبال النص المحول من المكتبة.

### الخطوة 3: الاتصال بالنموذج اللغوي (LLM API Call)

1.  استخدام مكتبة `http` لإرسال النص المحول إلى الـ **API** الخاص بالنموذج اللغوي المختار (مثلاً Gemini API).
2.  يجب تخزين مفتاح الـ API (API Key) بشكل آمن في التطبيق (يفضل استخدام متغيرات البيئة).
3.  استقبال الرد النصي من النموذج اللغوي.

### الخطوة 4: تحويل الرد إلى صوت (Text-to-Speech)

1.  تمرير الرد النصي الذي تم الحصول عليه من LLM إلى مكتبة `flutter_tts`.
2.  تشغيل الصوت الناتج للمستخدم تلقائياً.

### الخطوة 5: قياس الأداء

1.  بمجرد استلام الرد من LLM (الخطوة 3)، يجب تسجيل الوقت المستغرق للمعالجة.
2.  تخزين أوقات الاستجابة لعدة استفسارات.
3.  عرض **متوسط وقت الاستجابة (Average Response Time)** في سطر منفصل في ملف `README.md` هذا بعد إكمال المشروع للمقارنة.


## 📈 نتائج مقارنة الأداء (سيتم تحديثه لاحقاً)

| الإصدار | لغة/إطار العمل | متوسط وقت الاستجابة | ملاحظات |
| :--- | :--- | :--- | :--- |
| النسخة الحالية | **Flutter (Dart)** | *جاري القياس* | تهدف إلى توفير أداء سريع على منصات الجوال والويب. |
| النسخة المرجعية | Python | *جاري المقارنة* | المقارنة مع أداء الخادم (Server-side) في النسخة المرجعية. |
يمكنك الآن نسخ هذه الكتل (Blocks) ولصقها مباشرة في ملف README.md الخاص بك. هل تود إضافة أي تفاصيل أخرى أو تعديل أي جزء قبل النشر؟
